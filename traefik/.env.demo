# =============================================================================
# Environment Variables Template for Traefik + CrowdSec Setup
# =============================================================================
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all the required values below
# 3. Keep the .env file secure and never commit it to version control
# =============================================================================

# -----------------------------------------------------------------------------
# CLOUDFLARE API CREDENTIALS
# -----------------------------------------------------------------------------
# Traefik uses these credentials to create DNS records for SSL certificate
# validation via the DNS-01 challenge method.
#
# OPTION 1: Global API Key (less secure, but simpler)
# Get these from: Cloudflare Dashboard → My Profile → API Tokens → Global API Key
# CF_API_EMAIL=your-cloudflare-email@example.com
# CF_API_KEY=your-cloudflare-global-api-key-here

# OPTION 2: API Token (more secure, recommended for production)
# Create a token at: Cloudflare Dashboard → My Profile → API Tokens → Create Token
# Required permissions: Zone:DNS:Edit for your specific zone
# If using this option, comment out CF_API_EMAIL and CF_API_KEY above
CF_DNS_API_TOKEN='replace-me'

# -----------------------------------------------------------------------------
# OPTIONAL: TRAEFIK DASHBOARD CREDENTIALS
# -----------------------------------------------------------------------------
# These are set in docker-compose.yml but you can manage them here if preferred
# Generate a password hash using: echo $(htpasswd -nb admin yourpassword)
# Then replace the hash in docker-compose.yml traefik.http.middlewares.traefik-auth.basicauth.users
DASHBOARD_USER='admin'
DASHBOARD_PASSWORD_HASH='replace-me'

# -----------------------------------------------------------------------------
# OTHER THINGS
# -----------------------------------------------------------------------------
DOMAIN = 'example.com'